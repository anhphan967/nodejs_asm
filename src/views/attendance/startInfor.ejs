<%- include ('../includes/head.ejs')%>
    <%- include ('../includes/navbar.ejs')%>
        <%-include ('../includes/attendanceButton.ejs')%>
        <div class="row">
            <div class="col-md-4 mx-auto">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Tên Nhân Viên :</h4>
                        <hr>
                        <p class="card-subtitle mb-2 text-muted">
                            <%=staff.name%>
                        </p>
                        <hr>
                        <h4 class="card-title">Nơi Làm Việc: </h4>
                        <p class="card-subtitle mb-2 text-muted">
                            <%= lastStart.workPlace==='home' ? 'Nhà' : lastStart.workPlace==='company' ? 'Công ty'
                                : 'Khách Hàng' %>
                        </p>
                        <hr>
                        <h4 class="card-title">Giờ Bắt Đầu: </h4>                        
                        <p class="card-subtitle mb-2 text-muted">
                            <%= lastStart.startTime.toLocaleString()%>
                        </p>                        
                    </div>
                </div>
            </div>       
        </div>
        </div>
            <%- include ('../includes/end.ejs')%>